package com.EverythingBlocks.api;

/** Items that implement this interface can add custom information to the corresponding Everything Block, which will replace vanilla information */
public interface IReplaceEBInformation extends IAddEBInformation { }
